## Server only here for making connection possible after RTCPeerConnection if server creashes then also it will not touch the media 

#### RTCPeerConnection is the browser’s built-in engine that creates a secure, real-time, peer-to-peer connection between two users.